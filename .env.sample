# Environment variables for Veritas.O

# OpenAI API key (required for tribunal and audit jobs)
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI model configuration (required)
OPENAI_MODEL=gpt-4o-mini

# Optional: OpenAI project ID
OPENAI_PROJECT=optional-project-id

# Backend configuration
PORT=4000

# Optional: Growth engine settings
GROWTH_MODE=standard    # Options: standard, high_growth, emergency
GROWTH_CYCLE_DAY=1     # Day of week for growth cycles (0=Sunday)

# Optional: Agent temperature settings
AGENT_TEMP_ANALYTICAL=0.1
AGENT_TEMP_CREATIVE=0.3
AGENT_TEMP_BALANCED=0.2
